{
  "name": "conecopia",
  "description": "Django app for Conecopia",
  "scripts": {
    "dokku": {
      "predeploy": "cd order && python manage.py collectstatic --noinput && python manage.py migrate --noinput"
    }
  }
}