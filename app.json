{
  "name": "conecopia",
  "description": "Django app for Conecopia",
  "scripts": {
    "dokku": {
      "predeploy": "python order/manage.py collectstatic --noinput && python order/manage.py migrate --noinput"
    }
  }
}